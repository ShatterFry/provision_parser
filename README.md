Mobile provision parser, which decode developer certificate data for debugging purposes  
Usage: `python provision_parser.py game.ipa`  
OS dependencies: `python` is installed and in the PATH, `openssl` is installed and in the PATH to decode .mobileprovision binary file  
Python dependencies: cryptography package for decoding certificate data from base64 format
